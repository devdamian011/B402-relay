# Copy to .env.local and fill in real values locally. Never commit the filled-in file.
# NEXT_PUBLIC_* vars are exposed to the browser — no secrets here, only publishable keys/URLs.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
